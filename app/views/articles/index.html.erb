<h1>Listing articles</h1>

  <!-- <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
      <td><%= link_to 'Show', article_path(article) %></td>
      <td><%= link_to 'Edit', edit_article_path(article) %></td>
      <td><%= link_to 'Destroy', article_path(article),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %> -->

  <div class="row">
    <div class="col-md-2">Title</div>
    <div class="col-md-2">Text</div>
  </div>
  <% @articles.each do |article| %>
    <div class="row">
      <div class="col-md-2"><%= article.title %></div>
      <div class="col-md-2"><%= article.text %></div>
      <div class="col-md-1"><%= link_to 'Show', article_path(article) %></div>
      <div class="col-md-1"><%= link_to 'Edit', edit_article_path(article) %></div>
      <div class="col-md-1"><%= link_to 'Destroy', article_path(article),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></div>
    </div>
  <% end %>


<%= link_to 'New article', new_article_path %>
